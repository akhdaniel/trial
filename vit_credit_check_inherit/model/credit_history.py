#!/usr/bin/python
#-*- coding: utf-8 -*-

from odoo import models, fields, api, _
from odoo.exceptions import UserError, Warning

class credit_history(models.Model):
    _name = "vit.credit_history"
    _inherit = "vit.credit_history"
